version: '3.8'

services:
  frontend:
    build:
      context: ./equation-solver-source/equation-solver
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./equation-solver-backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

